# TaxOps AI 디자인 QA

## 최신 전체 UI/UX 검수 — 2026-08-31

첨부된 다섯 화면의 간격, 정렬, 모바일 AI 작업 영역과 잘림 문제를 재현한 뒤 기존 TaxOps AI 아키텍처와 EY 계열 테마 안에서 수정했다. 아래 접힌 이전 기록은 당시 조건에서의 판단이며, 현재 화면이나 모든 기기에 대한 통과 보증이 아니다.

로컬에서 단위 테스트 97건, 결정론적 AI 평가 45건과 Chromium E2E 31건을 통과했다. 시각 기준선을 고정한 전체 재실행, 타입 검사, 린트, 문서 포맷 검사와 프로덕션 빌드도 통과했다. 최초 배포 후 확인한 운영체제별 시각 기준선 차이와 초기 표 접근성 문제를 보완했고, Ubuntu 24.04에서도 전체 E2E 31건을 재시도 없이 통과했다. 아래 후속 검수에 실패 원인과 검수한 Linux 기준선의 출처를 기록한다.

배포 전 문구 검토에서는 `검토 의견 반영`, `자료 조회 범위`, `세무 업무 진행 단계`처럼 실무자가 다음 행동을 이해하기 쉬운 표현으로 정리했다. 단계 이름은 자료 수집, 근거 검토, 계산 및 초안, 검토 및 승인, 신고 및 사후 관리로 통일했다. 보안과 AI 품질 안내는 해시 체인 및 해당 평가 보고서가 실제로 검증하는 범위에 맞췄다. 세무 용어의 근거와 제품 용어의 구분은 [화면 문구 기준](docs/ui-terminology.md)에 기록했다. 아래의 로컬 전후 비교 이미지는 문구 정리 전 UI 구조 검수 기록이며, 최종 문구는 저장소의 시각 기준선과 공개 설명용 이미지에 반영한다.

### 시각 근거와 정규화

- 사용자 원본: 2026-08-31 오전 4.18.16, 4.18.29, 4.19.15, 4.19.27, 4.20.46 첨부 화면. 원본은 부분 캡처와 서로 다른 배율이 섞여 있으므로 직접 픽셀 오차를 계산하지 않았다.
- 같은 환경에서 재현한 수정 전 시각 기준: `artifacts/ux-audit/01-assistant-before-mobile.png`, `02-documents-before-mobile.png`, `03-cases-before-mobile.png`.
- 최종 구현: 같은 디렉터리의 `01-assistant-final-mobile.png`, `02-documents-final-mobile.png`, `03-cases-final-mobile.png`.
- 전체 나란히 비교: `01-assistant-final-comparison.png`, `02-documents-final-comparison.png`, `03-cases-final-comparison.png`. 각 소스는 390×650 CSS px, 390×650 이미지 px, 밀도 1x다. 배율 변경 없이 16px 간격을 둔 796×650 비교판에서 직접 검수했다.
- 집중 비교: `01-assistant-final-focus.png`. 두 화면의 하단 260px를 같은 배율로 잘라 796×260 비교판에서 입력창, 전송 버튼, 초안 안내와 하단 메뉴의 간섭을 확인했다.
- 상태: 같은 조직의 실무자, AI 초기 대화와 한빛테크 업무, 전체 자료 관리. 세무 업무 목록의 수정 전 캡처에는 앞선 입력 검증으로 생성된 업무가 포함되어 6건이며, 최종 캡처는 초기화한 예시 업무 4건이다. 이 비교에서는 건수와 과세기간 차이를 레이아웃 회귀로 판정하지 않았다.
- 전체 화면 근거: `artifacts/ux-audit/matrix-final-*-1440.png`, `matrix-final-*-390.png`. 검토 및 승인은 빈 목록이 아닌 검토자 계정의 실제 검토조서를 사용했다. 홈은 `matrix-final--1440.png`와 `matrix-final--390.png`다.
- 긴 모바일 화면은 원본을 720px씩 나눈 `*-390-tiles.png`로 상세 확인했다. 읽는 순서는 왼쪽에서 오른쪽, 위에서 아래다. 전체 페이지 캡처에 나타나는 고정 하단 메뉴 위치를 실제 콘텐츠 손실로 오판하지 않고, 조작 검증과 함께 판단했다.
- `artifacts/ux-audit`는 로컬 진단 산출물이다. 저장소에 남기는 재현 가능한 기준선은 `tests/e2e/visual-accessibility.spec.ts-snapshots/`이며 공개 설명용 이미지는 `artifacts/dashboard-visual.png`, `artifacts/assistant-visual.png`다.

### 발견 사항과 수정

| 등급 | 위치와 문제                                                                 | 수정 및 확인 근거                                                                                                                                                             |
| ---- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1   | 모바일 AI에서 고정 실행 맥락이 화면 대부분을 점유하고 대화·입력 안내를 가림 | 작은 업무 맥락, 대화/참고 근거 두 탭, 접이식 실행 안내로 재구성. 390×650에서 대화 영역이 약 68px에서 273px로 증가. 초안 안내 하단 575.3px, 하단 메뉴 시작 584px로 겹치지 않음 |
| P1   | 짧은 화면과 화면 키보드에서 AI 입력창 또는 하단 조작부가 잘림               | `visualViewport` 높이와 위치, 크기 제한된 대화 스크롤, 독립 입력 영역 사용. 키보드 및 낮은 높이에서는 탐색 영역 축소. 6개 크기, 2,000자 입력, 축소된 가용 높이를 검사         |
| P2   | 근거·첨부·검색 대화상자의 화면 밖 표시, 포커스 이탈                         | 공통 네이티브 `Dialog`로 통합. 상단 표시 영역과 본문 스크롤, 고정 승인 버튼, Escape, Tab 순환과 호출 버튼으로의 포커스 복귀 검사                                              |
| P2   | 자료 요약 카드가 불필요하게 늘어나고 아이콘과 수치가 어긋남                 | 요약 카드의 늘어남을 막고 상단 정렬, 아이콘 중앙 배치, 작은 화면 단일 열 적용. 공통 지표 카드의 수치 정렬과 레이블 줄 높이도 보정                                             |
| P2   | 업무별 자료 탭과 표의 간격 부족, 모바일 열 잘림                             | 탭 간격과 조작 높이 확보. 업무·자료·운영·평가·감사 표를 필드명이 있는 모바일 레코드로 재배치하고 긴 텍스트 줄바꿈. 업무 열기 버튼과 상태 배지 정렬 보정                       |
| P2   | 1024px에서 업무 표의 숨김 헤더가 페이지 폭을 늘림                           | 절대 위치인 화면 낭독기용 헤더의 기준을 `TableViewport`로 제한. 실제 페이지 폭 1,161px→1,024px 확인. 표 안의 가로 스크롤과 키보드 접근, 스크롤 안내는 유지                    |
| P2   | 태블릿 상단 검색·상태·새 업무 버튼이 눌려 줄바꿈                            | 검색 영역이 남은 폭에 맞춰 줄어들도록 하고 상태와 행동 버튼의 폭 유지. 실제 1024px 화면에서 한 줄 정렬 확인                                                                   |
| P2   | 전체 자료 화면에서 업로드 대상 선택 경로 부족, AI 첨부 시 대화 이탈         | 업무 선택 후 업로드하도록 구성. AI 안에서 같은 업로드 패널을 열어 대화와 질문 초안 유지. 업로드 후 목록 갱신, 중복 실행과 파일 크기 검사                                      |
| P2   | 업무 등록의 단계별 검증과 다음 단계 포커스 불안정                           | 단계별 스키마에 해당하는 필드만 검증하고 최종 단계는 전체 계약 검증. 필수값, 중복 제출, 담당 검토자, 단계 이동과 완료 후 검색 반영 검사                                       |
| P2   | 검토 완료 후 대기 건수·다른 조서의 처리 상태가 일치하지 않음                | 조서 ID별 결정 상태 관리와 실행 잠금. 승인 후 목록 수치와 상태, 재조회, 산출물 해시·승인 토큰 경계 검사                                                                       |
| P2   | 근거 미리보기 실패 시 복구 동작 부족                                        | 30초 대기 제한, 닫거나 재조회할 때 이전 요청 취소, 한국어 오류와 재시도 버튼 제공. 내용을 읽지 못한 상태에서는 승인 불가                                                      |
| P2   | 새 업무가 검색에 즉시 반영되지 않고 한국어 조합 입력 중 이동 가능           | 검색을 열 때 업무 목록 갱신, 조회 실패 재시도, IME 조합 중 Enter 이동 차단                                                                                                    |
| P2   | 감사 로그의 검색·필터·내보내기 동작 부족                                    | 실제 검색어와 결과 필터, 결과 건수, 빈 상태 초기화, 현재 결과 CSV 내보내기 구현. CSV 인용과 수식 주입 방어를 공통 함수로 유지                                                 |
| P2   | 이전 AI 메시지에 최신 메시지의 근거가 전달됨                                | 각 응답에 속한 스트리밍 근거만 해당 메시지에 전달                                                                                                                             |
| P2   | AI API 오류가 JSON으로 노출되고 실패한 도구가 계속 실행 중처럼 보임         | 오류 안내를 한국어 메시지로 표시. 실패·실행 거부·승인 대기·실행 중 상태를 구분하고 실패 스피너 제거. 오류 복구와 렌더링 단위 검사                                             |
| P2   | 평가 그래프 높이와 통과 기준이 실제 보고서와 다름                           | 임의로 40을 빼던 막대를 제거하고 0~100% `meter`와 지표별 실제 기준 사용. 24시간 운영 예시의 기간 설명도 일치시킴                                                              |
| P2   | 대비, 중복 랜드마크 이름, 키보드로 접근할 수 없는 스크롤 영역               | 단계 번호·범례 대비, 탐색·자료 요약 영역 이름, 표와 모달 본문 포커스 보정. 10개 화면의 데스크톱·모바일 Axe 위반 0건                                                           |
| P3   | 계산식의 불완전한 마지막 행, 빈 상태 위계와 오류 식별 안내                  | 계산식 전체 행 배치, 빈 상태의 명확한 제목과 간격, 존재하지 않는 업무의 h1, 오류 코드가 있을 때 표시                                                                          |

### 필수 시각 검수

- 글꼴: 기존 시스템 글꼴과 EY 계열 타이포그래피 위계 유지. 보조 텍스트 12px 이상, 주요 업무 본문 14px, 모바일 입력 16px를 실제 렌더링으로 검사했다. 한글 줄바꿈과 긴 파일명·해시 표시를 확인했다.
- 간격: 입력창과 탐색 영역의 경계를 분리하고, 필터·카드·표·모달에 일관된 여백을 적용했다. 모바일에서 작은 글씨로 정보를 억지로 맞추지 않고 세로 읽기와 필요한 내부 스크롤을 사용한다.
- 색상: 노랑·차콜 브랜드와 기존 성공·주의·실패 토큰을 유지했다. 정보는 색상만이 아니라 상태 텍스트와 수치를 함께 표시한다.
- 자산: 기존 TaxOps 마크와 Lucide 아이콘을 유지했다. 새 사진·일러스트나 임의 대체 자산은 만들지 않았다. 자료 카드의 아이콘 비율과 정렬을 확인했다.
- 문구: 대화, 참고 근거, 전문가 검토 전 초안, 연결할 세무 업무, 검색 결과 내보내기 등 행동과 상태를 구분했다. 운영 화면의 예시 데이터와 AI 평가의 제한된 회귀 검증 범위를 그대로 명시한다.

### 재검수 이력

1. 첨부 화면 및 390×650 재현: 대화 영역 붕괴와 입력 안내 가림을 확인해 차단 상태로 시작했다. 이전 390×844 중심 판단으로 낮은 화면까지 통과했다고 간주하지 않았다.
2. 구조 수정 후 전 화면 검사: 대비, 랜드마크, 스크롤 키보드 접근과 태블릿 넘침을 추가 확인했다. 원인을 수정한 뒤 동일 크기에서 다시 캡처했다.
3. 첫 통합 실행에서 근거 조회 대기시간 초과와 AI 오류 테스트의 중복 `alert` 선택이 발생했다. 요청 대기 상태를 추적으로 확인하고 조회 복구를 보강했으며, 테스트는 앱 오류 영역만 선택하도록 고쳤다. 단독 재검사와 후속 전체 실행에서 통과했다.
4. 최종 모바일 전체 비교판 및 입력창 집중 비교판, 데스크톱·모바일 전 화면을 검수했다. 의도된 변경을 확인한 후 대시보드와 AI 시각 기준선을 갱신했고 전체 E2E 31건이 통과했다.
5. 기준선 갱신 옵션 없이 전체 E2E 31건을 다시 실행해 통과했다. 존재하지 않는 업무와 일반 404의 제목, 목록 복귀 경로도 브라우저에서 별도로 확인했다.
6. 최초 배포 `8d2fbc1`의 Linux CI에서는 핵심 업무 흐름 22건과 모바일 AI 시각 검사가 통과했으나, Mac 기준선을 공유한 홈·업무 화면 2건이 실패했다. 홈 높이 1,898px 대 1,921px, 업무 화면 픽셀 차이 16%를 확인했다. 운영체제별 기준선 분리와 Ubuntu 24.04 실행 환경 고정으로 보완했다. 표가 서버에서 처음 렌더링된 순간의 키보드 접근 누락도 수정하고 단위 검사를 추가했다. 이 시점에는 Linux 후보 검수가 남아 후속 검수를 차단 상태로 기록했다.
7. 첫 Linux 후보는 자동 검사 31건을 통과했지만, 접근성 검사 이후 캡처된 업무 화면의 상단 메뉴 일부가 잘려 있어 승인하지 않았다. 진입 화면 캡처를 접근성 검사 전에 수행하고 메뉴 버튼 전체가 화면 안에 표시되는지 검사하도록 수정했다. [Linux 최종 후보 실행](https://github.com/kwakhyun/taxops-ai/actions/runs/33335508714)에서 세 후보 생성과 기준선을 갱신하지 않는 전체 E2E 31건이 모두 재시도 없이 통과했다. 홈 1440×1921, 업무·AI 화면 각 390×844의 한글 표시, 줄바꿈, 상단 메뉴와 하단 조작부를 직접 검수한 뒤 `linux` 기준선 세 장을 확정했다. Mac 전체 31건 및 캡처 순서 변경 화면 2건도 기존 기준선으로 통과했다. 자동 검수의 픽셀 허용 오차와 접근성 검사 항목은 완화하지 않았다.

### 검증 범위

- 업무 화면 10개: 홈, 세무 업무 목록, 업무 등록, 개별 업무, 자료 관리, AI 파트너, 검토 및 승인, 운영 현황, AI 품질 평가, 감사 로그.
- 화면 크기 6개: 1440×900, 1024×768, 768×1024, 390×650, 320×568, 844×390. AI 입력창은 1440×720도 별도로 검사한다.
- 접근성: 데스크톱·모바일 전 화면 Axe, 검색과 근거 검토 모달, AI 근거 탭. 키보드 메뉴, 포커스 복귀, 표 스크롤과 모바일 입력 가독성을 별도로 검사한다.
- 핵심 흐름: 등록, 검색, 다운로드, 업로드, 인용 응답, 근거 부족 보류, 응답 중단·재생성, 첨부 중 초안 유지, 승인·재조회, 감사 검색·CSV, 공식 MCP 클라이언트. 잘못된 파일, 프롬프트 공격, 위조 해시와 승인 토큰 재사용 거부도 유지한다.
- 단위·코드 계약 97/97, AI 평가 45/45. 커버리지 하한 통과: 구문 50%, 분기 44%, 함수 49.4%, 행 50.71%.
- 이 결과는 Chromium과 데모 데이터 기준이다. 실제 iOS/Android 키보드·보조기술, 실제 모델, 운영 DB·외부 서비스·클라우드 부하 검증을 수행했다는 의미가 아니다. 화면 키보드는 가용 영역 변화 모의 검사이며 실기기 시험은 아니다.
- 배포 여부는 Git 커밋과 Vercel 배포 기록에서 별도로 확인한다.

### 구현 체크리스트

- [x] 기존 서버·DB·권한·승인 경계 유지
- [x] 낮은 모바일 AI 화면, 긴 입력, 첨부와 근거 확인 개선
- [x] 전체 화면 간격, 표, 모달, 지표와 오류 복구 개선
- [x] 모든 업무 화면의 6개 크기 및 주요 역할 확인
- [x] 원본/수정 후 비교와 의도된 시각 기준선 갱신
- [x] 고정 기준선 재실행, 문서 포맷 검사와 프로덕션 빌드 최종 확인

<details>
<summary>이전 비교와 가독성 검수 기록 — 당시 범위만 유효</summary>

## 비교 대상

- 원본 시각 기준
  - 데스크톱: `/private/tmp/tax-intelligence-home.png` — Tax Intelligence 포트폴리오 관제 화면, 1440 × 1482px
  - 모바일: `/private/tmp/tax-intelligence-mobile.png` — Tax Intelligence AI 업무 화면, 390 × 1674px
- 구현 화면
  - 데스크톱: `tests/e2e/visual-accessibility.spec.ts-snapshots/dashboard-desktop.png` — TaxOps AI 관제 화면, 1440 × 1896px
  - 모바일 Engagement: `tests/e2e/visual-accessibility.spec.ts-snapshots/engagement-mobile.png` — 390 × 844px
  - 모바일 AI 파트너: `tests/e2e/visual-accessibility.spec.ts-snapshots/assistant-mobile.png` — 390 × 844px
- 한 화면 비교 근거
  - 전체 구성: `artifacts/design-qa-desktop-comparison.png` — 왼쪽 원본, 오른쪽 구현
  - 모바일 집중 영역: `artifacts/design-qa-mobile-comparison.png` — 왼쪽 원본 상단 844px, 오른쪽 구현

## 정규화와 상태

- 데스크톱은 1440px CSS 폭, device scale factor 1로 비교했다. 원본과 구현의 전체 문서 높이는 각각 1482px와 1896px이며, 전체 구성 비교판에서는 양쪽을 720px 폭으로 동일 비율 축소하고 상단 정렬했다. 구현 뷰포트는 1440 × 1000 CSS px이다.
- 모바일은 390 × 844 CSS px, device scale factor 1로 맞췄다. 원본 390 × 1674px 중 동일한 상단 844px를 잘라 구현의 현재 뷰포트와 1:1로 배치했다.
- 데스크톱 상태는 예시 조직의 로그인 후 홈 화면이다. 모바일 상태는 `한빛테크 주식회사` 부가가치세 업무를 선택하고 AI 파트너 탭을 연 상태다.
- 원본과 구현은 서로 다른 제품 구조이므로 고객명과 세무 업무 내용은 같지 않다. 픽셀 복제가 아니라 Tax Intelligence의 우수한 정보 밀도, Engagement 진행 맥락과 모바일 AI 작업 구조가 TaxOps AI의 권한·근거 중심 아키텍처 안에서 동등하게 구현됐는지를 판단했다.

## Findings

- 현재 남은 P0, P1, P2 불일치는 없다.
- [수용된 차이] 데스크톱 내비게이션은 원본의 상단형 대신 TaxOps AI의 역할 기반 사이드바를 유지했다. 관제 지표, 우선 처리 업무, 일정, Engagement 흐름의 계층은 원본보다 높은 정보 밀도로 흡수했고 기존 권한 구조를 손상하지 않는다.
- [수용된 차이] 원본 모바일은 단일 업무의 마감과 초안 생성 CTA를 크게 노출한다. 구현은 AI 화면에서 고객사, 세목, 과세기간, 자료 수와 검색 경계를 우선 노출하고, 마감과 제출 준비 상태는 세무 업무 화면에서 제공한다. 두 경로는 고정 하단 내비게이션으로 한 번에 전환된다.
- [P3] 원본의 AI 질문 입력은 화면 상단부터 넓은 작성 면적을 제공한다. 구현은 실행 경계와 근거 부족 시 답변 보류 원칙을 먼저 보여 줘 첫 입력 영역이 조금 낮다. 민감한 세무 AI의 운영 맥락을 우선한다는 제품 판단으로 수용했다.

## 필수 충실도 점검

- 글꼴과 타이포그래피: 기존 TaxOps AI 시스템 글꼴과 숫자 위계를 유지했다. 원본의 굵은 업무 제목, 작은 보조 정보와 대문자 섹션 레이블 계층을 관제 브리핑과 AI 파트너에 반영했다. 390px에서 제목이나 탭이 잘리지 않는다.
- 간격과 레이아웃 리듬: 데스크톱은 지표 4열, 브리핑·일정 2열, 업무·검토 2열과 6단계 여정을 일정한 8px 기반 간격으로 구성했다. 모바일은 한 열로 재배치하고 44px 이상의 주요 조작 영역과 고정 하단 내비게이션 여백을 확보했다.
- 색상과 토큰: EY 계열의 검정, 차콜과 노랑을 핵심 강조에 사용하고 위험, 주의, 완료는 기존 의미 색상으로 제한했다. Axe 검사에서 색상 대비 위반이 없도록 중간 회색과 위험 텍스트 토큰을 조정했다.
- 이미지와 자산 품질: 두 화면 모두 제품 사진이나 일러스트를 요구하지 않는 업무형 UI다. 기존 TaxOps 로고와 Lucide 아이콘 계열을 일관되게 사용했으며 임시 이미지, 이모지, CSS 도형 또는 사용자 정의 SVG 대체물은 없다.
- 문구와 콘텐츠: `업무 개요 → 요청·자료 → 근거·리서치 → 계산·초안 → 검토·승인 → 제출·사후`를 세무 Engagement의 공통 정보구조로 사용한다. AI 화면은 `답변`, `근거`, `AI 파트너`, `현재 업무 자료`, `현재 조직으로 제한`, `전문가 검토 전 초안`으로 실행 경계를 명확히 표시한다.
- 아이콘과 상태: 아이콘 크기와 선 굵기는 기존 Lucide 체계에 맞췄다. 현재 메뉴와 단계는 `aria-current`, 진행률은 이름 있는 progressbar, 장식 아이콘은 접근성 트리 제외를 사용한다.

## 비교 이력

### 1차 검수 — blocked

- [P1] 모바일 하단 메뉴가 고정 5열 그리드를 사용해 권한상 4개 메뉴만 보이는 역할에서 빈 열이 남았다.
  - 수정: 메뉴 수에 따라 동일 폭을 나누는 flex 구조로 변경했다.
- [P1] 모바일 AI 화면에서 기존 데스크톱 숨김 규칙이 실행 맥락 패널까지 숨겨 검색 범위와 조직 경계가 보이지 않았다.
  - 수정: 모바일 전용 탭과 실행 맥락 패널을 추가하고 답변, 근거, AI 파트너 상태를 실제 버튼으로 전환하도록 구현했다.
- [P1] Axe 검사에서 중간 회색과 위험 텍스트 대비, 이름 없는 검색 버튼, 금지된 ARIA 사용이 확인됐다.
  - 수정: 색상 토큰, 버튼 이름, 장식 아이콘과 비활성 다운로드 표현을 보정했다.
- [P2] 개발 서버 표시기가 시각 기준선에 포함됐다.
  - 수정: Next.js `devIndicators`를 비활성화하고 기준선을 다시 캡처했다.

### 2차 검수 — passed

- 수정 후 데스크톱 전체 구성과 모바일 AI 집중 영역을 각각 원본과 구현이 함께 있는 비교판에서 재검수했다.
- 후속 근거: `artifacts/design-qa-desktop-comparison.png`, `artifacts/design-qa-mobile-comparison.png`.
- 390px 모바일에서 업무 맥락, 세 탭, AI 실행 경계, 질문 입력과 역할별 하단 메뉴가 겹치거나 잘리지 않는다.
- 데스크톱에서 포트폴리오 브리핑, 예정 일정, 우선 처리 업무, 검토 현황, Engagement 흐름과 AI 진입점이 동일한 시각 계층 안에 유지된다.

## 상호작용과 기술 검증

- 주요 상호작용: 통합 검색, 업무 이동, 6단계 Engagement 링크, 모바일 하단 메뉴, AI 답변·근거·AI 파트너 탭, 질문 입력을 확인했다.
- 브라우저 콘솔: 데스크톱 대시보드, 모바일 세무 업무와 모바일 AI 파트너에서 오류와 경고가 없음을 확인했다.
- 접근성: 세 기준 화면에서 `@axe-core/playwright` 전체 규칙 위반 0건을 확인했다.
- 시각 회귀: 데스크톱 1440 × 1000, 모바일 390 × 844 기준선을 Playwright 스냅샷으로 고정했다.

## Implementation Checklist

- [x] 기존 TaxOps AI 서버·권한·데이터 경계 유지
- [x] 데스크톱 관제 정보 밀도와 일정 구조 흡수
- [x] 6단계 Engagement 공통 정보구조 적용
- [x] 권한 대응 모바일 내비게이션과 AI 작업 탭 구현
- [x] Axe 접근성 및 데스크톱·모바일 시각 회귀 테스트 추가
- [x] 문서와 포트폴리오 이미지 갱신
- [x] 공통 글자 크기, 한글 줄바꿈과 모바일 입력 가독성 보정

## 가독성 후속 검수 — 2026-08-31

### 원본과 수정 후 근거

- 이 요청에서 새로 캡처한 수정 전 화면: `artifacts/typography-before-dashboard.png`와 `artifacts/typography-before-mobile.png`.
- 수정 후 화면: `artifacts/typography-after-dashboard.png`, `artifacts/typography-after-mobile.png`, `artifacts/typography-after-evidence.png`.
- 전체 비교판: `artifacts/typography-comparison-dashboard.png`. 양쪽 모두 1440 × 1000 CSS px 및 이미지 픽셀, 밀도 1x이며 비교판에서만 720 × 500px로 같은 비율 축소했다.
- 집중 비교판: `artifacts/typography-comparison-mobile.png`. 양쪽 모두 390 × 844 CSS px 및 이미지 픽셀, 밀도 1x이며 확대·축소 없이 나란히 비교했다.
- 같은 조직과 예시 데이터를 사용한 홈 및 AI 파트너 초기 상태다. 기존 EY 테마, 화면 구조, 문구와 기능은 그대로 유지했다.

### 비교 이력과 수정

1. [P1, 수정 완료] 관제 표, Engagement 설명, AI 실행 정보와 입력 글자 수에 7~11px가 사용됐다. 이전 Axe 검사는 최소 글자 크기를 판정하지 않아 이를 차단하지 못했다. `--font-size-*` 공통 토큰으로 최소 12px, 표 본문과 업무명 14px, 상태 배지와 주요 레이블 13px, 모바일 입력 16px를 적용했다.
2. [P2, 수정 완료] 커진 글씨로 모바일 빈 상태 설명의 마지막 줄이 바로 가려지고 근거 정보에서 한글 단어가 중간에 끊겼다. 장식 아이콘과 상단 여백을 줄이고 한국어 단어 단위 줄바꿈을 적용해 읽을 공간을 확보했다. 업무 설명과 근거 제목의 불필요한 한 줄 말줄임도 제거했다.
3. 수정 후 비교판에서 글꼴 계열과 제목 위계, 간격, 색상 토큰, 아이콘 선명도, 실제 업무 문구를 재검수했다. 색상·자산·문구 변경 없이 핵심 텍스트의 크기와 줄 간격을 개선했다. 모바일 근거 탭과 검색 입력을 직접 확인했고 브라우저 콘솔 오류는 없었다.

### 재발 방지

- 10개 업무 화면 × 3개 폭(1440px, 390px, 320px)에서 렌더링된 최소 글자 크기, 모바일 입력 크기와 페이지 가로 넘침을 검사한다.
- 세 시각 기준 화면에는 같은 가독성 검사와 Axe 검사를 함께 적용한다. 이 검증은 전체 WCAG 준수나 모든 보조기술 호환성을 보증하지 않는다.
- 본문 스크롤과 Engagement 가로 스크롤은 정보 접근을 위한 의도된 동작이며, 글씨를 줄여 화면에 끼워 넣지 않는다.

## 전체 UI 재검수 시작 기록 — 2026-08-31

이전 검수는 390 × 844 화면과 제한된 상태를 대상으로 했다. 이번 검수에서 390 × 650의 AI 대화 영역 축소와 고정 메뉴 겹침을 재현했으므로, 이전의 통과 판단을 전체 화면이나 모든 높이의 보증으로 해석하지 않는다.

### 품질 평가 시각화 계약

- 질문: 현재 평가 보고서의 5개 품질 지표가 각각의 통과 기준을 충족하는가?
- 근거: `artifacts/evaluation-report.json`의 `metrics`와 `thresholds`. 검색, 답변 근거 일치, 주장 무결성, 답변 보류, 공격 차단의 5개 항목을 동일한 0~100% 단위로 비교한다. 새로운 수치나 시계열을 만들지 않는다.
- 발견: 기존 막대 높이는 `value - 40`으로 축약되고, 100% 기준인 지표에도 고정 90% 선이 표시됐다. 운영 차트는 00~22시 자료를 최근 12시간으로 설명했다.
- 구현 표면: 기존 Next.js 업무 앱의 기본 HTML `meter` 위젯과 정확한 숫자 레이블. 별도 보고서, 외부 차트 런타임, 배포 산출물은 만들지 않는다.
- 표현: 긴 한국어 지표명을 읽기 쉬운 가로형 비교, 0% 기점, 지표별 통과 기준과 실제 값 직접 표시. 기존 제품의 품질 상태 색상을 사용하되 텍스트 값과 기준을 함께 제공해 색에만 의존하지 않는다.
- 검증: JSON 값과 화면의 meter `value`, `min`, `max`, `low` 비교 및 모바일·데스크톱 캡처. 한정된 45개 회귀 사례가 실제 세무 정확성을 보증하지 않는다는 기존 설명을 유지한다.

</details>

최종 UI 검수 차단 항목 없음. 실제 모바일 운영체제의 키보드와 보조기술, 운영 연동은 위 제한 사항에 따라 별도 검증이 필요하다. 최종 커밋의 전체 CI와 배포 상태는 GitHub Actions 및 Vercel 기록에서 확인한다.

final result: passed
