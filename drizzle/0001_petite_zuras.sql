-- Snapshot reconciliation only. The baseline migration was security-reviewed and
-- already contains these schema objects together with their RLS policies, scoped
-- foreign keys, grants, and immutable-data triggers.
SELECT 1;
